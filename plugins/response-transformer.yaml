plugins:
    - name: response-transformer-advanced
      service: ${service_name}
      config:
        remove:
          headers:
          - X-Powered-By
          - Server
          - X-AspNet-Version
